# Frontend (safe to expose — public anon key, RLS restricts writes)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Digest script only — NEVER put this in the frontend / commit it
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# OPENAI_API_KEY=sk-...
